# DEC-20260912-001 — Public-default secret handling: record only repo display name (active_root), never absolute path; full path stays in local stderr only.

- Category: process
- Session: ?
- Project: dumble

## Verbatim (original)

> This story explicitly marks the repo public — public-default applies. The full absolute path WILL NOT be included in any record — active_root stores only the repo display name; the full path appears only in local stderr.

## Verbatim (English)

> This story explicitly marks the repo public — public-default applies. The full absolute path WILL NOT be included in any record — active_root stores only the repo display name; the full path appears only in local stderr.

## Rationale

Absolute path leaks OS username even when the story is cleared for public.
